import './App.css';
import { Button } from '@material-ui/core';

const App = () => {
  return (
    <div>
      <Button variant='contained' color='primary'>Este es un bóton</Button>
      <Button variant='outlined' color='secondary'>Hello</Button>
    </div>
  );
};

export default App;
